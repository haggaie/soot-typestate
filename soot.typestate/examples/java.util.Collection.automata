package java.util;

automaton Collection {
	initial = EMPTY;
	state EMPTY {
		boolean add(Object) -> NON_EMPTY;
		boolean addAll(Collection) -> NON_EMPTY;
	}
	
	state NON_EMPTY {
		void clear() -> EMPTY;
	}
}